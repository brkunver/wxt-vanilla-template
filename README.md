# wxt-vanilla-template

Template files for the `wxt` (vanilla) starter, used by the Kunver CLI.

Template files live in the `template/` subfolder. Files starting with `_`
(e.g. `_gitignore`) are restored to dotfiles (`.gitignore`) by the CLI at
scaffold time.
